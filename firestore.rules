rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // 【開発・連携用設定】
    // 管理アプリ側との接続を優先するため、一時的に全公開に設定しています。
    // 本番公開時には、write権限を特定の管理者（Authユーザー）に制限することを推奨します。
    
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
