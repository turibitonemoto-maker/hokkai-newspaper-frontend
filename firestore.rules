rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // 管理者権限の判定ロジック。
    // 指定された管理者メールアドレス、または @hgu.jp ドメインのアカウントを許可。
    function isAuthorizedEditor() {
      return request.auth != null && 
             request.auth.token.email != null && (
               request.auth.token.email.lower().endsWith('@hgu.jp') || 
               request.auth.token.email == "turibitonemoto@gmail.com" ||
               request.auth.token.email == "admin@example.com"
             );
    }

    // 記事：読み取りは全員、書き込み・削除は許可されたメンバーのみ
    match /articles/{articleId} {
      allow read: if true;
      allow create, update, delete: if isAuthorizedEditor();
    }

    // カテゴリー：読み取りは全員、書き込み・削除は許可されたメンバーのみ
    match /categories/{categoryId} {
      allow read: if true;
      allow create, update, delete: if isAuthorizedEditor();
    }

    // ヒーロー画像：読み取りは全員、書き込み・削除は許可されたメンバーのみ
    match /hero-images/{imageId} {
      allow read: if true;
      allow create, update, delete: if isAuthorizedEditor();
    }

    // デフォルトはすべて拒否
    match /{path=**} {
      allow read, write: if false;
    }
  }
}