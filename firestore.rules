
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // 全てのドキュメントの読み取りを無条件で許可（閲覧専用・認証不要）
    match /{document=**} {
      allow read: if true;
      allow write: if false;
    }
    
    // 最終更新: 2024-03-08T11:00:00Z (Announcementsタブ追加に伴うパブリック閲覧の徹底)
  }
}
